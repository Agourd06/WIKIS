<?php

if (!define("HOST", "localhost")) {

    define("HOST", "localhost");
}
if (!define("DB", "wikis")) {

    define("DB", "wikis");
}
if (!define("USER", "root")) {

    define("USER", "root");
}
if (!define("password", "")) {

    define("password", "");
}

define("URLROOT", "http://localhost/WIKIS/");
?>